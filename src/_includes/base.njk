<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="{{ description or 'IA-ismo LAB — Laboratorio de pensamiento crítico en la intersección entre inteligencia artificial y humanidades.' }}">
  <meta name="author" content="IA-ismo LAB">
  
  <!-- Open Graph -->
  <meta property="og:title" content="{{ title }}">
  <meta property="og:description" content="{{ description or 'El archivo canónico del pensamiento IAista.' }}">
  <meta property="og:type" content="{{ 'article' if layout == 'post' else 'website' }}">
  
  <title>{{ title }}{% if title != 'IA-ismo LAB' %} | ⚠️ IA-ismo LAB{% endif %}</title>
  
  <link rel="stylesheet" href="{{ '/css/style.css' | url }}">
  <link rel="icon" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>⚠️</text></svg>">
</head>
<body>
  
  <header class="site-header">
    <div class="header-content">
      <div class="site-branding">
        <h1 class="site-title">
          <a href="{{ '/' | url }}" title="⚠️ IA-ismo LAB">⚠️ IA-ismo LAB</a>
        </h1>
        <p class="site-tagline">» Archivo • Pensamiento • Crítica</p>
      </div>
      <nav class="nav-main">
        <a href="{{ '/' | url }}" {% if page.url == "/" %}class="active"{% endif %}>INICIO</a>
        <a href="{{ '/proyectos' | url }}" {% if page.url == "/proyectos/" %}class="active"{% endif %}>proyectos</a>
        <a href="{{ '/articulos' | url }}" {% if '/articulos' in page.url or '/posts' in page.url %}class="active"{% endif %}>ARTÍCULOS</a>
        <a href="{{ '/videos' | url }}" {% if page.url == "/videos/" %}class="active"{% endif %}>VÍDEOS</a>
        <a href="{{ '/cineai' | url }}" {% if page.url == "/cineai/" %}class="active"{% endif %}>CINEAI</a>
      </nav>
    </div>
  </header>

  <main class="site-main">
    {{ content | safe }}
  </main>

  <footer class="site-footer">
    <div class="footer-content">
      <p>⚠️ IA-ismo LAB &copy; 2025</p>
      <div class="footer-links">
        <a href="https://github.com/IA-ismo-Lab/iaismo.com" target="_blank" rel="noopener">GitHub</a>
        <a href="mailto:contacto@iaismo.com">Contacto</a>
      </div>
      <p class="footer-quote">
        "Hecho por una humana y muchas IAs flotantes."
      </p>
    </div>
  </footer>

</body>
</html>
